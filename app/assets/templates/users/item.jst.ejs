<a href=<%="#/users/"+ user.escape('id')%>> <%= user.escape('username') %> </a>
<% if ( parseInt(user.escape('id')) !=  Sonichunt.currentUser.id){ %>
  <button class="follow-button">
<% if ( user.follow().isNew() ){ %>
  Follow
<% }else {%>
  Unfollow
<% }}%>
</button>
